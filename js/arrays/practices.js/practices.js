
const start = parseInt(prompt("Enter the start value of the range:"));
const end = parseInt(prompt("Enter the end value of the range:"));


    for (let i = start; i <= end; i++) {
        if (i % 2 !== 0)
             { 
            console.log(i);
             }
    }

const start1 = parseInt(prompt("Enter the start value of the range:"));
const end1 = parseInt(prompt("Enter the end value of the range:"));


for (let i = start1; i <= end1; i++) {
    if (i % 3 == 0 && i%6==0)
         { 
        console.log(i);
         }  
}

